//SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

import "./libraries/LibStorage.sol";

/**
 * @dev This contract is used when we need to update the storage after a diamondCut
 */
contract Init {
    function init() external {
        // do something
    }
}
